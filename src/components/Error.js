import React from 'react';

const Error = () => {
    return(
        <section id="content">
            <h2 className="subheaer">Pagina no encontrada</h2>
            <p>La pagina en la que intesnta accede no existe en la web.</p>
        </section>
    );
}

export default Error;
